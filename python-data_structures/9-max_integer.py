#!/usr/bin/python3

from queue import Empty


def max_integer(my_list=[]):
    max = 0
    if list is not Empty:
        for i in my_list:
            if i > max:
                max = i
        return max
    return None
